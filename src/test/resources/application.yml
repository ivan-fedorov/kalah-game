bot:
  telegram:
    name: "kalah_game_bot"
    token: ${TELEGRAM_TOKEN}

spring:
  data:
    mongodb:
      username: test_user
      password: test_password
      database: kalah
      host: localhost
      authentication-database: admin
